<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ================================================================
         تسجيل كتاب وارد - the mail room, in six fields.
         ================================================================ -->
    <record id="view_legal_correspondence_register_wizard_form" model="ir.ui.view">
        <field name="name">legal.correspondence.register.wizard.form</field>
        <field name="model">legal.correspondence.register.wizard</field>
        <field name="arch" type="xml">
            <form string="Register Incoming Letter">
                <group>
                    <group string="The Letter">
                        <field name="gov_body_id"/>
                        <field name="body_section"/>
                        <field name="their_number" placeholder="e.g. K/M/1234"/>
                        <field name="their_date"/>
                        <field name="subject" placeholder="Tax assessment for 2025"/>
                    </group>
                    <group string="Our Entry">
                        <field name="register_id"/>
                        <field name="kind_id"/>
                        <field name="our_date"/>
                        <field name="secrecy"/>
                        <field name="document_action"/>
                        <field name="entity_id"/>
                        <field name="reply_to_id"/>
                    </group>
                </group>
                <group string="Scans">
                    <field name="attachment_ids" widget="many2many_binary" nolabel="1"/>
                </group>
                <field name="note" placeholder="Remark on receipt" nolabel="1"/>
                <footer>
                    <button name="action_register" type="object" string="Register And Allocate Number"
                            class="btn-primary" data-hotkey="q"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_legal_correspondence_register_wizard" model="ir.actions.act_window">
        <field name="name">Register Incoming Letter</field>
        <field name="res_model">legal.correspondence.register.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- ================================================================
         إلغاء قيد - the line drawn across the page.
         ================================================================ -->
    <record id="view_legal_correspondence_void_wizard_form" model="ir.ui.view">
        <field name="name">legal.correspondence.void.wizard.form</field>
        <field name="model">legal.correspondence.void.wizard</field>
        <field name="arch" type="xml">
            <form string="Void Entry">
                <group>
                    <field name="correspondence_id" invisible="1"/>
                    <field name="our_number"/>
                    <field name="subject"/>
                    <field name="replacement_id"/>
                </group>
                <field name="reason" placeholder="The reason: a duplicate letter, refused at the window, or never sent."/>
                <div class="alert alert-warning" role="alert">
                    The number stays in the book and is never reused. A voided entry keeps its
                    place exactly as a struck-through line keeps its place in the paper register -
                    otherwise the book has a gap nobody can explain years later.
                </div>
                <footer>
                    <button name="action_void" type="object" string="Confirm Void"
                            class="btn-primary" data-hotkey="q"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- ================================================================
         تدوين اتصال - what the counter actually said.
         ================================================================ -->
    <record id="view_legal_contact_note_wizard_form" model="ir.ui.view">
        <field name="name">legal.contact.note.wizard.form</field>
        <field name="model">legal.contact.note.wizard</field>
        <field name="arch" type="xml">
            <form string="Record Contact Note">
                <group>
                    <group>
                        <field name="correspondence_id"/>
                        <field name="kind_id"/>
                        <field name="gov_body_id"/>
                        <field name="body_section"/>
                    </group>
                    <group>
                        <field name="contact_date"/>
                        <field name="spoke_to" placeholder="Abu Ahmed - deputy head of the section"/>
                        <field name="promised_on"/>
                        <field name="entity_id"/>
                        <field name="subject"/>
                    </group>
                </group>
                <field name="said" placeholder="He said the file is with the legal adviser, and promised to look at it after Eid."/>
                <div class="alert alert-info" role="alert">
                    This note takes no register number - it never went through the book at all.
                    But the promised date moves the reply deadline onto it and stops the next
                    chase, so the system does not keep chasing a body that has already answered.
                </div>
                <footer>
                    <button name="action_record" type="object" string="Save Note"
                            class="btn-primary" data-hotkey="q"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_legal_contact_note_wizard" model="ir.actions.act_window">
        <field name="name">Record Contact Note</field>
        <field name="res_model">legal.contact.note.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
